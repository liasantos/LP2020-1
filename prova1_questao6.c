/* Escreva um programa que leia um número inteiro de até quatro dígitos e o decomponha para imprimir na tela a quantidade de milhares, centenas, dezenas e unidades que compõem o número. //Entrada: um número inteiro de até 4 dígitos //Saída: a decomposição dos números */

// #include <stdio.h>
// int main(){
//   int numero, milhar, centena, dezena, unidade;
//   scanf("%i", &numero);
//   milhar = numero/1000;  
//   centena = numero%1000/100;
//   dezena = numero%1000%100/10;
//   unidade = numero%1000%100%10;
//   printf("%i milhar(es)\n", milhar);
//   printf("%i centena(s)\n", centena);
//   printf("%i dezena(s)\n", dezena);
//   printf("%i unidade(s)\n", unidade);
//   return 0;
// }